{
  "type": "Label",
  "description": "Icon + text label; title next to a system or asset image",
  "id": { "policy": "optional" },
  "inheritsViewProperties": true,
  "topLevelKeys": [],
  "ownProperties": {
    "title":         { "types": ["string"], "required": false, "description": "Label text" },
    "systemImage":   { "types": ["string"], "required": false, "description": "SF Symbol name" },
    "imageName":     { "types": ["string"], "required": false, "description": "Asset image name from Assets.xcassets" },
    "imageScale":    { "types": ["string"], "required": false, "enum": ["small","medium","large"], "description": "Icon rendering scale" },
    "materialName":  { "types": ["string"], "required": false, "description": "(Android only; author as materialName:android) Material Symbol name, the Android stand-in for systemImage" },
    "materialWeight":{ "types": ["number"], "required": false, "description": "(Android only) Material Symbols 'wght' axis, 100..700; defaults to 400" },
    "materialFill":  { "types": ["number","boolean"], "required": false, "description": "(Android only) Material Symbols 'FILL' axis, 0..1 (or boolean); defaults to 0" },
    "materialGrade": { "types": ["number"], "required": false, "description": "(Android only) Material Symbols 'GRAD' axis, -50..200; defaults to 0" },
    "materialSize":  { "types": ["number"], "required": false, "description": "(Android only) explicit icon size in sp; defaults to the imageScale-scaled ambient font size" }
  },
  "observable": {}
}
