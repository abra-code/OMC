{
  "type": "Group",
  "description": "A transparent layout container; inherits layout from its parent. Supports static children or data-driven template mode.",
  "id": { "policy": "optional" },
  "inheritsViewProperties": true,
  "topLevelKeys": ["children", "template"],
  "children": { "required": false, "description": "Static child views" },
  "template":  { "required": false, "description": "Template rendered once per data row; set rows via setElementRows" },
  "ownProperties": {},
  "observable": {
    "states.content": { "types": ["array"], "description": "All data rows as [[String]]; use getElementRows/setElementRows" }
  }
}
