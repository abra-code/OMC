{
  "type": "LazyHGrid",
  "description": "Horizontal lazy-loading grid; children are arranged into fixed rows that scroll horizontally.",
  "id": { "policy": "optional" },
  "inheritsViewProperties": true,
  "topLevelKeys": ["children"],
  "children": { "required": false, "description": "Child views placed into the grid" },
  "ownProperties": {
    "rows": {
      "types": ["array"],
      "required": false,
      "description": "Row definitions; each entry is {minimum: number} for fixed height or {flexible: true} for flexible; default [flexible]",
      "items": {
        "types": ["object"],
        "properties": {
          "minimum":  { "types": ["number"],  "required": false, "description": "Fixed row height in points" },
          "flexible": { "types": ["boolean"], "required": false, "description": "When true, row takes available space" }
        }
      }
    },
    "spacing":   { "types": ["number"], "required": false, "description": "Spacing between grid items; default 0" },
    "alignment": { "types": ["string"], "required": false, "enum": ["top","center","bottom"], "description": "Vertical alignment of items; default 'center'" }
  },
  "observable": {}
}
