{
  "type": "NavigationLink",
  "description": "Push-navigation link. Uses 'destinationViewId' (inherited View property) to reference a destination in a NavigationStack.",
  "id": { "policy": "optional" },
  "inheritsViewProperties": true,
  "topLevelKeys": ["destination"],
  "destination": { "required": false, "description": "Inline destination view (Form 1). Alternatively use destinationViewId in properties." },
  "ownProperties": {
    "title":       { "types": ["string"], "required": false, "description": "Link label text" },
    "systemImage": { "types": ["string"], "required": false, "description": "SF Symbol name for a leading link icon" },
    "materialName":  { "types": ["string"], "required": false, "description": "(Android only; author as materialName:android) Material Symbol name, the Android stand-in for systemImage" },
    "materialWeight":{ "types": ["number"], "required": false, "description": "(Android only) Material Symbols 'wght' axis, 100..700; defaults to 400" },
    "materialFill":  { "types": ["number","boolean"], "required": false, "description": "(Android only) Material Symbols 'FILL' axis, 0..1 (or boolean); defaults to 0" },
    "materialGrade": { "types": ["number"], "required": false, "description": "(Android only) Material Symbols 'GRAD' axis, -50..200; defaults to 0" },
    "materialSize":  { "types": ["number"], "required": false, "description": "(Android only) explicit icon size in sp; defaults to the inherited font size" }
  },
  "observable": {}
}
