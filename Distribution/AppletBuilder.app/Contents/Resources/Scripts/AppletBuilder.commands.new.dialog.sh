#!/bin/bash
# AppletBuilder.commands.new.dialog - Opens the New Command dialog
# The dialog is opened by the ACTIONUI_WINDOW declaration in Command.plist

echo "[$(/usr/bin/basename "$0")]"
env | sort

exit 0
